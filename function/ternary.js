// -------------type-1-------------

let userType;
let age = 22;

if (age <= 18) {
    userType = "child";
} else {
    userType = "Adult";
}

document.write(`<h1>${userType}</h1>`);
// -----------------type-1-end----------------

// ------------type-2--------------------

let agee = 22;
let userTypes = agee >= 18 ? 'child' : 'Adult';

document.write(`<h1>${userTypes}</h1>`);

// --------------type--2---end------------